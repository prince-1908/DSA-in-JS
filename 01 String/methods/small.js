// Return value
// A string beginning with a <small> start tag, then the text str, and then a </small> end tag.

const str = "prince"
console.log(str.small());

// document.body.innerHTML = str.small();