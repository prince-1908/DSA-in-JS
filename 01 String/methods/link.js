const str = "prince.pinu"
// A string beginning with an <a href="url"> start tag (double quotes in url are replaced with &quot;), then the text str, and then an </a> end tag.

console.log(str.link("prince.pinu.com"))